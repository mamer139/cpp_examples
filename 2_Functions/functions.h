#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <vector>
#include <string>

// Function declarations
void greet();
int add(int a, int b);
double multiply(double a, double b);
void printVector(const std::vector<int>& vec);

#endif // FUNCTIONS_H
